Run once (requires network, ~15–20 min for 591 images):

```bash
npm run download:assets
# or: python scripts/download_assets.py
```

Verify after download:

```bash
npm run verify:m1
```
- `id-name.json`
- `manifest.json`
- `images/*.png` (~591 files)
